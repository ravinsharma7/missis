# Specification change log

## 2026-08-15 — v2 supersedes v1

`specs/missues-issue-specificationv2.md` is the canonical specification.

`specs/missues-issue-specification.z.v1.md` is retained for history only and is
superseded. Any conflict between the two files resolves in favor of v2.

The v1 filename carries `.z.v1` so file listings and reviewers can see it is
the older revision, not the active contract.
